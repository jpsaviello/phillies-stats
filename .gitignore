# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
.vercel
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env*

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Playwright smoke tests: reports and per-run artifacts. The fixtures under
# tests/fixtures/ ARE committed — they're what makes the suite hermetic.
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Locally linked agent skills. `npx impeccable link` symlinks this to a checkout
# OUTSIDE the repo, so committing it would push a link that resolves to nothing
# on every other machine. Each developer links it themselves. If you ever
# install the skill as real files here and want them committed, drop this line
# or use `git add -f`.
.claude/skills/impeccable
